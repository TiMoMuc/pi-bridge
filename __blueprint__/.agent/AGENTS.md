# Workspace AGENTS

This file is your workbench. Keep it useful for your future self.

## Session start
- A workspace-orientation block from `.agent/orient.py` may already be present in your system prompt. Treat it as informational context, not as law.
- If you need to re-orient later and the user's request is not urgent, run `python /workspace/.agent/orient.py`.
- For a fresh map of the workspace, use flags such as `--repo-tree`, `--skills`, or `--events`.
- Trust the output of `.agent/orient.py` and your tools over stale assumptions about files, skills, or events.
- If the user's need is urgent, help first and orient later.

## Working style
- Keep durable notes here or in files you deliberately maintain from here.
- Use orientation output to discover structure quickly, then inspect the relevant files directly.
- Treat skills as deliberate reads: orientation can list available skills, but inspect a full `SKILL.md` only when the current task actually needs it.
- Remove notes that no longer help.
- Leave the workspace clearer than you found it.

## Notes

(empty)
